interface Config {
  discord_invite_code: string;
  schedule: string;
}

/**
 * These variables are expected to change, possibly semester-by-semester.
 */
const config: Config = {
  discord_invite_code: '2nBDKJkVUS',
  schedule: 'COR B112',
};

export default config;
