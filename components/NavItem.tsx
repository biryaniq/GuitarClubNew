import React from "react";
import styled from "@emotion/styled";
import Link from "next/link";

const FlexContainer = styled.div`
  display: flex;
  align-content: center;
`;

const NavItem = (NavigationLink) => {
  <Link href={}>
    <FlexContainer>
    
    
    </FlexContainer>
  </Link>
}

export default NavItem;